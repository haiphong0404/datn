const initialState = {
    products: [], // Đảm bảo đây là đúng
    // Các thuộc tính khác
  };
  
  // Reducer
  const productReducer = (state = initialState, action) => {
    switch (action.type) {
      // Các case khác
      default:
        return state;
    }
  };
  